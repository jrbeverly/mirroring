#!/bin/bash
DIR="$( dirname $( cd "$( dirname "${BASH_SOURCE[0]}" )" && pwd ) )"

## Need a list of them, and templating

# Mass import all of the projects
#terraform import gitlab_project.example richardc/example
